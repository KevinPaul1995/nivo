---
import { faqItems } from "../../data/siteContent";
---

<section class="content-section faq-section" aria-labelledby="faq-title" data-parallax-section>
  <div class="section-grid section-grid--split">
    <div class="parallax-layer parallax-layer--copy">
      <div class="section-copy">
        <span class="section-eyebrow" data-i18n="faq.eyebrow">Preguntas frecuentes</span>
        <h2 id="faq-title" data-i18n="faq.title">Lo básico para coordinar tu limpieza.</h2>
      </div>
    </div>

    <div class="parallax-layer parallax-layer--panel">
      <div class="faq-list">
        {
          faqItems.map((item) => (
            <details>
              <summary data-i18n={`faq.question.${item.question}`}>{item.question}</summary>
              <p data-i18n={`faq.answer.${item.question}`}>{item.answer}</p>
            </details>
          ))
        }
      </div>
    </div>
  </div>

  <button class="section-scroll-cue" type="button" data-scroll-next aria-label="Ir a la siguiente sección">
    <span aria-hidden="true"></span>
  </button>
</section>
