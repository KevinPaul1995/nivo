---
import { site } from "../../data/siteContent";
---

<section class="content-section contact-section" id="contacto" aria-labelledby="contact-title" data-parallax-section>
  <div class="parallax-layer parallax-layer--panel">
    <div class="contact-card">
      <div>
        <span class="section-eyebrow" data-i18n="contact.eyebrow">Contacto</span>
        <h2 id="contact-title" data-i18n="contact.title">Cotiza tu limpieza en minutos.</h2>
        <p data-i18n="contact.text">
          Usa la cotización guiada para obtener un valor inicial y luego confirma
          los detalles por WhatsApp.
        </p>
      </div>

      <div class="contact-actions">
        <a class="button quote-cta" href="/cotizar/">
          <span data-i18n="contact.cta">Cotiza con nosotros</span>
        </a>
        <a class="button button--light" href={site.contact.whatsappHref} target="_blank" rel="noreferrer" data-i18n="contact.whatsapp">
          {site.contact.whatsappLabel}
        </a>
        <a class="button button--ghost contact-actions__email" href={`mailto:${site.contact.email}`}>
          {site.contact.email}
        </a>
      </div>
    </div>
  </div>
</section>
